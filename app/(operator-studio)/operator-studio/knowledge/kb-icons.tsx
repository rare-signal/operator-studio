/**
 * Heroicon-style names re-exported from lucide-react.
 *
 * The KB UI was modeled 1:1 on AIDA Observatory (Svelte + Heroicons).
 * Operator Studio uses lucide-react throughout; this file maps the
 * names AIDA used to their lucide equivalents so the JSX reads the
 * same as the source it ports from.
 */
export {
  Sparkles,
  Search as MagnifyingGlass,
  Star,
  ArrowRight,
  Clock,
  Lightbulb as LightBulb,
  BarChart as ChartBar,
  Calculator,
  FileText as DocumentText,
  User,
  Scale,
  AlertTriangle as ExclamationTriangle,
  ClipboardCheck as ClipboardDocumentCheck,
  ShieldCheck,
  BadgeCheck as CheckBadge,
  RefreshCw as ArrowPath,
  Pencil as PencilSquare,
  CheckCircle2 as CheckCircle,
  Files as DocumentDuplicate,
  Link,
  Tag,
  Plus,
  LayoutGrid as Squares2x2,
  FileSearch as DocumentMagnifyingGlass,
} from "lucide-react"
